%p
  %strong Recibo:
  = @enrollment.voucher
%p
  %strong Activo:
  = @enrollment.active
%p
  %strong Estudiante:
  = @enrollment.student.person.name
= link_to 'Editar', edit_cycle_enrollment_path(params[:cycle_id], @enrollment.id)
|
= link_to 'Regresar', cycle_path(params[:cycle_id])
