%h1 Personas
%table
  %thead
    %tr
      %th Apellidos
      %th Nombres
      %th DNI
      %th Teléfono
      %th Correo electrónico
      %th Género
      %th Estado
      %th Rol
      %th{:colspan => "3"}
  %tbody
    - @people.each do |person|
      %tr
        %td= person.lastname
        %td= person.name
        %td= person.dni
        %td= person.phone
        %td= person.email
        %td= person.gender
        %td= person.state
        %td= person.rol
        %td= link_to 'Mostrar', person
        %td= link_to 'Editar', edit_person_path(person)
        %td= link_to 'Eliminar', person, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'Nueva Persona', new_person_path
